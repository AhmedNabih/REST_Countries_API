version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/install-deps
      - run:
          command: chmod u+rwx ./API_Tests/manage.py
      - store_test_results:
          path: test-results

workflows:
  main:
    jobs:
      - build-and-test